# План реализации проекта — People Counting & Flow

> Тема 14 (Human Sys.): подсчёт людей и оценка направления движения на видео
> пешеходного перехода. Полностью автоматическая система по пайплайну
> `image → enhance → segment → clean → detect → decide`.

Этот документ — единый трекер фаз. Статусы обновляются по мере работы.

---

## Инструкция по запуску (ВАЖНО)

Виртуальное окружение `.venv` использует Python **3.14**, а системный `py` — 3.13,
где не установлен `cv2`. Поэтому **запуск только через интерпретатор из `.venv`**:

```powershell
# Из корня проекта C:\Users\akric\PycharmProjects\cv_project
$env:PYTHONIOENCODING='utf-8'
.venv\Scripts\python.exe main.py --video data/test.mp4 --display
```

Либо сначала активировать окружение, затем `python`:

```powershell
.venv\Scripts\Activate.ps1
python main.py --video data/test.mp4 --display
```

НЕ запускать через `py main.py ...` — будет `ModuleNotFoundError: No module named 'cv2'`.

Тестовое видео: `data/test.mp4` — 1080×1920, 30 fps, 1764 кадра (~59 с), плотный
пешеходный переход.

---

## Роли команды (3 человека)

| Роль | Участник | Стадии |
| :-- | :-- | :-- |
| Lead CV Engineer | Витя | Detect + Decision + Integration + Data |
| Image Processing Specialist | Александр | Enhance + Segment |
| Morphology & Report Lead | Богдан | Clean + Visualization + Report |

---

## Диагноз проблемы (почему квадраты мелькают / счёт недостоверен)

1. **Классика (MOG2 + контуры) непригодна для плотной толпы**: силуэты сливаются
   в один блоб, зебра и шум дают ложные срабатывания, стоящие люди исчезают.
2. **Главная причина мелькания — самописный `CentroidTracker`**: ID к кадру 600
   доходят до #1300+ (огромная текучка). Жадное сопоставление только по дистанции,
   `max_distance=80` px на кадре высотой 1920, `max_missing=5`, нет предсказания
   движения → перекрытие = потеря ID = новый бокс = мелькание.
3. **YOLOv8n пропускает мелких/дальних людей** на полном кадре 1080×1920.
4. **Счёт недостоверен by design**: при потере/смене ID у линии возможен двойной
   подсчёт или недосчёт; нет ground truth для проверки.

---

## Фазы

### Фаза 0 — Запуск и фиксация baseline ✅ ВЫПОЛНЕНО
- [x] Зафиксировать запуск только через `.venv\Scripts\python.exe`.
- [x] Прогон baseline (классический режим, контуры + CentroidTracker).
- [x] Console-output пользователя совпал с baseline (это классический режим;
      строка «Creating new Ultralytics Settings» появляется из-за импорта
      `detect_yolo`, который тянет `ultralytics` даже без флага `--yolo`).

**Baseline (классический режим, контуры + CentroidTracker):**
```
line_y=960, size=1080x1920
Frames processed: 1644 (после 120 кадров warmup)
Count IN:  40
Count OUT: 46
Runtime:   ~236 c (~4 мин, CPU)
```
Вывод: для перехода с сотнями пересечений за ~55 с цифры явно недостоверны —
подтверждает диагноз. Эти кадры/числа идут в отчёт как failure case.
Отдельного `--yolo` baseline нет: прогон пользователя был в классическом режиме.

### Фаза 1 — Переработка трекинга и счёта ✅ ВЫПОЛНЕНО
Главная фаза: устраняет мелькание и делает счёт достоверным.
Детальный план: см. `docs/PHASE_1.md`.
- [x] Стабильные ID трекинга (ByteTrack через `model.track(persist=True)`).
      Реализовано в `src/track_yolo.py` с `bytetrack_custom.yaml` (track_buffer=90).
- [x] Переписать `decision.py`: счёт по стабильным ID, каждый ID — один раз.
      Добавлен `LineCounter` — source-agnostic счётчик по ID.
      Включён отложенный подсчёт при смерти трека (first_cy vs last cy).
- [x] Линия/зона с направлением (IN/OUT + flow).
- [x] Визуализация по трекам (`visualize_tracks`) — уменьшает мерцание боксов.

**Результат YOLO-режима (yolov8s, imgsz=1280, conf=0.35):**
```
Frames processed: 1644
Count IN:  38
Count OUT: 25
Unique IDs: 193
```
Ручной подсчёт: IN 49 / OUT 32. Основная причина недосчёта — пропуски дальнего плана.

### Фаза 2 — Улучшение детекции ✅ ВЫПОЛНЕНО
- [x] Модель `yolov8s.pt` (вместо `yolov8n`).
- [x] `imgsz=1280` (вместо дефолтных 640) — улучшает детекцию дальнего плана.
- [x] `bytetrack_custom.yaml` с `track_buffer=90` для уменьшения текучки ID.
- [ ] ~~ROI по зоне перехода~~ — не реализовано, не критично.

### Фаза 3 — Улучшение классического пути ✅ ВЫПОЛНЕНО
- [x] `segment.py`: `detectShadows=True`, `varThreshold=32.0`.
- [x] `clean.py`: добавлен `filter_components` (connected components с фильтром
      по площади и aspect ratio: min_area=1500, max_area=30000, min_aspect=1.2).
- [x] `enhance.py`: добавлен `enhance_denoise` (Gaussian blur ksize=5 перед MOG2).

**Результат классики (улучшенной):**
```
Frames processed: 1644
Count IN:  32
Count OUT: 20
```
Классика по-прежнему ловит ложные объекты (листья, стены, машины), боксы мерцают.
Это ожидаемый failure case — MOG2 ловит любое движение, а не людей.

**Сводка результатов на `data/test.mp4`:**

| Метод | IN | OUT | Комментарий |
| :-- | :-- | :-- | :-- |
| Baseline (MOG2 + CentroidTracker, без улучшений) | 40 | 46 | ID 1300+, недостоверно |
| Классика улучшенная (денойз, тени, фильтр) | 32 | 20 | Ложные срабатывания, мерцание |
| YOLO(s) + ByteTrack + LineCounter | 38 | 25 | Пропуски дальнего плана |
| Ручной подсчёт | 49 | 32 | Ground truth |

### Фаза 4 — Сравнение методов (бонус +5) ✅ ВЫПОЛНЕНО
- [x] Benchmark-скрипт (`benchmark.py`): прогон обоих методов → сравнительная таблица.

**Результат бенчмарка (`benchmark.py`, одно видео, одинаковые условия):**

| Метод | IN | OUT | IDs | Время |
| :-- | :-- | :-- | :-- | :-- |
| Classic (MOG2 + CentroidTracker) | 37 | 27 | 1041 | 248.6s |
| YOLO(s) + ByteTrack + LineCounter | 38 | 25 | 193 | 322.6s |
| Ground truth (ручной подсчёт) | 49 | 32 | — | — |

**Ground truth (ручной подсчёт полного видео): IN 49 / OUT 32.**

**Выводы:**
- YOLO даёт в **5× меньше текучки ID** (193 vs 1041), что означает значительно
  более стабильный трекинг.
- По точности OUT: YOLO 25 vs GT 32 (−22%), классика 27 vs GT 32 (−16%).
  По IN: YOLO 38 vs GT 49 (−22%), классика 37 vs GT 49 (−24%).
  Оба метода недосчитывают, но по разным причинам: классика — ложные
  срабатывания + неработающий трекер, YOLO — пропуски детекции на дальнем плане.
- YOLO медленнее (~323s vs ~249s) из-за inference нейросети на CPU.
- Классика ловит ложные объекты (листья, стены, машины) — фундаментальное
  ограничение MOG2 (реагирует на любое движение, а не только на людей).

### Фаза 5 — Валидация счёта ✅ ВЫПОЛНЕНО
- [x] Ручной подсчёт IN/OUT на полном видео → ground truth: **IN 49 / OUT 32**.
- [x] Сравнение авто-счёта с ground truth:

| Метод | IN (err%) | OUT (err%) |
| :-- | :-- | :-- |
| Classic (MOG2 + CentroidTracker) | 37 (−24%) | 27 (−16%) |
| YOLO(s) + ByteTrack + LineCounter | 38 (−22%) | 25 (−22%) |
| Ground truth | 49 | 32 |

### Фаза 6 — Вывод и отчёт
- [ ] 6 обязательных выходов на тест-кадр (есть `save_stage_interval`).
- [ ] PDF: проблема → пайплайн → методы → стадийные картинки → failure cases →
      сравнение методов → вывод.
- [ ] Contribution Statement.

---

## Метод работы

Пользователь пишет код сам. Агент даёт материал (теория + практика/примеры) и
обновляет документацию (docs/*.md). Идём по фазам последовательно. Текущая фаза — **6**.
